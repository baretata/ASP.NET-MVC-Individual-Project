@model RecruitYourself.Web.Areas.Article.ViewModels.ArticleViewModel

@{
    ViewBag.Title = "Details";
}
<div class="row">
    <div class="box">
        <div class="item-container col-lg-12">
            <hr>
            <h2 class="intro-text text-center">
                @Model.Name
            </h2>
            <hr>
            <img class="img-responsive img-border img-left" src="@Model.Image" alt="Article Image">
            <hr class="visible-xs">
            <p>@Model.Content</p>
        </div>
        <span class="pull-right">by <em>@Model.Creator</em> on <em>@Model.CreatedOn</em></span>
        <p>
            @Html.ActionLink("Back to All Articles", "Index", null, new { @class = "btn btn-default" })
        </p>
    </div>
</div>
